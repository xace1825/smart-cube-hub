# Smart Cube Hub Frontend

React + TypeScript + Vite + TailwindCSS.

## Запуск

```bash
npm install
npm run dev
```

Открыть в Chrome/Edge на `http://localhost:5173`.

Web Bluetooth работает только с HTTPS (или localhost) и только в Chromium-браузерах.
